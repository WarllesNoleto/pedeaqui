import { Navigate, Route, Routes } from 'react-router-dom';import { useAuthStore } from './store/auth';import { DashboardLayout } from './ui/DashboardLayout';import { LoginPage, ProductsPage, PDVPage, OrdersPage, KitchenPage, CashierPage, PublicMenuPage, CompaniesPage } from './screens';export function AppRoutes(){const token=useAuthStore(s=>s.token);return <Routes><Route path='/login' element={<LoginPage/>}/><Route path='/cardapio/:slug' element={<PublicMenuPage/>}/><Route path='/' element={token?<DashboardLayout/>:<Navigate to='/login'/>}><Route index element={<PDVPage/>}/><Route path='empresas' element={<CompaniesPage/>}/><Route path='produtos' element={<ProductsPage/>}/><Route path='pedidos' element={<OrdersPage/>}/><Route path='cozinha' element={<KitchenPage/>}/><Route path='caixa' element={<CashierPage/>}/></Route></Routes>}
